using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
namespace Valve.VR.InteractionSystem.Sample
{

    public class ElementManager : MonoBehaviour
    {
        //public Dropdown elementsDropdown;

        //public void CreateNewElement()
        //{
        //    Element element = new Element ();
        //    Xml_Manager.ins.curriculum.academicYears[0].semsters[0].subjects[0].lessons[0].elements.Add(element);
        //    //need edit , index static
        //    AddNewOptionToDropdown(Xml_Manager.ins.curriculum.academicYears[0].semsters[0].subjects[0].lessons[0]);
        //}

        //void AddNewOptionToDropdown(Lesson lesson)
        //{
        //    List<string> dropdownData = new List<string>();
        //    dropdownData.Add(lesson.elements.Count.ToString());
        //    elementsDropdown.AddOptions(dropdownData);
        //}
    }
}